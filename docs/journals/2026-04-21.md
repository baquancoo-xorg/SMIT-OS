# 2026-04-21

## Lead Logs u2014 Implementation (17:35)

**Status:** Completed u2014 0 TypeScript errors in changed files

### Files
- **Created:** `src/components/lead-tracker/lead-detail-modal.tsx` u2014 read-only popup, status badge, 2-col field grid, `whitespace-pre-wrap` notes
- **Created:** `src/components/lead-tracker/csv-export.ts` u2014 RFC 4180, UTF-8 BOM blob, date-stamped filename
- **Modified:** `src/components/lead-tracker/lead-logs-tab.tsx` u2014 per-cell inline edit u00d75 fields, customer name click u2192 modal, Export CSV button

### Key technical decisions
- `defaultValue` (uncontrolled) for inline inputs u2014 no extra state needed, save-on-blur is immediate
- `saveInlineField` uses functional setState `setInlineEdit(cur => ...)` to prevent async save from closing a newly-opened edit
- UQ Reason edit only activates when `lead.status === 'Unqualified'`
- Row-level edit (editId) and per-cell inline edit are mutually exclusive by JSX structure

---

## Lead Logs v2 u2014 Implementation (18:18)

**Status:** Completed u2014 all 3 phases, 0 TypeScript errors in changed files

### Phase 01 u2014 Backend Audit Trail
- Added `LeadAuditLog` Prisma model: `id`, `leadId`, `changes` (Json), `createdAt`; `@@index([leadId, createdAt])`; `onDelete: Cascade`
- `npm run db:push` u2014 migration applied in 102ms
- `normalizeVal()` helper normalizes date fields to `YYYY-MM-DD` string before diff comparison (avoids `Date` vs string mismatch)
- PUT `/:id` now fetches `existing` before update, diffs 7 tracked fields, writes `LeadAuditLog` only when `Object.keys(changes).length > 0`
- GET `/:id/audit` added as static route before `/:id` dynamic route (ordering critical)

### Phase 02 u2014 Frontend Quick Wins
- `lead-logs-tab.tsx`: Removed Export CSV + `exporting` state. Added AE dropdown inline edit, Received Date date-input inline edit. Notes `<input>` u2192 `<textarea rows={2}>` with Enter=save / Shift+Enter=newline / Escape=cancel. Added `Modified` column with `date-fns` format `dd/MM/yyyy - HH:mm`. Updated colSpan 9/10 u2192 10/11.
- `LeadTracker.tsx`: Export CSV button moved to header (between Paste from Excel and Add Row), gated on `isSale && activeTab === 'logs'`

### Phase 03 u2014 Timeline Modal
- `LeadAuditLog` interface added to `src/types/index.ts`
- `getLeadAuditLogs(id)` added to `src/lib/api.ts`
- `lead-detail-modal.tsx` rewritten: `useEffect` fetches on `lead?.id` change, `FIELD_LABEL` map for human-readable keys, `fmtDiffVal()` converts `YYYY-MM-DD` u2192 `DD/MM/YYYY` in diff display. Modal u2192 `flex flex-col max-h-[80vh]` with scrollable body. Timeline shows timestamp + per-field fromu2192to diffs.

### TypeScript note
`Object.entries(log.changes)` value needed explicit cast `as { from: string | null; to: string | null }` u2014 Prisma Json type comes through as `{}` at compile time.

---

## Lead Logs v2 u2014 Brainstorm + Plan (17:56)

**Plan:** `plans/260421-1756-lead-logs-v2-inline-edit-audit-trail/`

### Features planned
1. **Move Export CSV button** u2014 from filter bar u2192 header next to ADD ROW
2. **AE inline edit** u2014 dropdown from Sale user list (aeOptions already loaded)
3. **Received Date inline edit** u2014 same pattern as Resolved Date
4. **Notes textarea** u2014 Shift+Enter newline, Enter saves, Escape cancels
5. **Last Modified column** u2014 `updatedAt` already in DB/API, format `dd/MM/yyyy - HH:mm`
6. **Audit trail timeline** u2014 new `LeadAuditLog` Prisma model, diff on PUT, timeline in Detail Modal

### Key decisions
- Timeline does NOT track `changedBy` u2014 user decision, simplifies backend
- History starts from deploy date (no backfill)
- `updatedAt` already exists (`@updatedAt`) u2014 no extra migration for Last Modified column
- Phase 02 (frontend quick wins) independent of Phase 01 (backend) u2014 can run in parallel
- Phase 03 (timeline modal) blocked by Phase 01
- Modal card u2192 `flex flex-col max-h-[80vh]` to support scrollable timeline
- Date values in audit diff normalized to `DD/MM/YYYY` (not ISO) for readability

### Files (new plan)
- Modify: `prisma/schema.prisma`, `server/routes/lead.routes.ts`, `src/lib/api.ts`, `src/types/index.ts`
- Modify: `lead-logs-tab.tsx`, `lead-detail-modal.tsx`, `LeadTracker.tsx`

---

## Group Board Filters u2014 Brainstorm + Plan (18:20)

**Plan:** `plans/260421-1820-group-board-assigned-status-filter/`

### Problem
TechBoard / MarketingBoard / MediaBoard / SaleBoard chu1ec9 cu00f3 Sprint filter. Cu1ea7n thu00eam Assigned + Status filter cu1ea1nh Sprint trong stats bar, u1ea3nh hu01b0u1edfng cu1ea3 kanban, stats counts, vu00e0 table view.

### Key decisions
- Extract `useGroupBoardFilters` hook (DRY) thay vu00ec thu00eam state vu00e0o 4 file gu1ea7n giu1ed1ng nhau
- Assigned options: chu1ec9 users cu00f3 dept khu1edbp vu1edbi board (tu1eeb `useAuth().users`)
- Filter chain: sprint u2192 status u2192 assignee (AND logic, client-side)
- Table view bug fixed: hiu1ec7n nhu1eadn `items` raw bu1ecf qua sprint filter u2192 u0111u1ed5i thu00e0nh `filteredItems`
- Stats counts reflect tu1ea5t cu1ea3 filter u0111ang active

### Phases
1. `use-group-board-filters.ts` hook u2014 encapsulate state + computed filteredItems
2. Update 4 boards u2014 import hook, thu00eam 2 CustomFilter UI, fix kanban + table

---

## Lead Logs u2014 Brainstorm & Plan

**Plan:** `plans/260421-1722-lead-logs-inline-edit-export/`

### Features planned
1. **Lead Detail Modal** u2014 click customer name u2192 read-only popup, full notes
2. **Per-cell inline edit** u2014 5 fields (Status, Resolved, Lead Type, UQ Reason, Notes); click u2192 control inline, auto-save
3. **Export CSV** u2014 all DB records, UTF-8 BOM, client-side generation

### Key decisions
- Per-cell > row-level toggle for inline edit UX
- `csv-export.ts` extracted to keep `lead-logs-tab.tsx` < 500 lines
- Row-level edit (Edit2 icon) kept only for Customer + AE
- CSV export calls `api.getLeads()` with no filter (no pagination)

### Files
- Create: `lead-detail-modal.tsx`, `csv-export.ts`
- Modify: `lead-logs-tab.tsx`
